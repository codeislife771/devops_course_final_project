groups:
  - name: test
    rules:
      - alert: TestAlertUIOnly
        expr: vector(1)
        for: 1m
        labels:
          severity: info
        annotations:
          summary: "UI test alert"
          description: "Fires after ~1 minute to verify the alerting pipeline (Prometheus → Alertmanager)"
